#!/usr/bin/env bash
# This script creates a backup of all your databases

sudo mysqldump -uroot --password="$1" --all-databases > backup.mysql
backupFile=$(date "+%d-%m-%Y").tar.gz
tar -cvzf "$backupFile" backup.mysql
